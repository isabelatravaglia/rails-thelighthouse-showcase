<%= simple_form_for(@article) do |f| %>

  <!-- [...] -->

  <div class="form-inputs">
    <%= f.input :title, label: "Título" %>
    <%= f.input :photo, as: :file, label: "Imagem", input_html: { multiple: false } %>
  </div>

  <div class="form-inputs">
    
    <%= f.association :article_categories,  as: :check_boxes, label: "Categorias", collection: ArticleCategory.order(:name), label_method: :name, value_method: :id %>
  </div>

  <!-- insert these three line of code below -->
  <div class="form-inputs">
    <%= f.rich_text_area :rich_body %>
  </div>

  <div class="form-actions pt-3">
    <%= f.button :submit, f.object.new_record? ? 'Criar Artigo' : 'Atualizar', class: "btn btn-info purple inverted fixwidth" %>
  </div>
<% end %>